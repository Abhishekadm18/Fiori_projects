sap.ui.define([
    "sap/ui/core/mvc/Controller"
],
function (Controller) {
    "use strict";

    return Controller.extend("fioridashborad.controller.View1", {
        onInit: function () {
            // var oModel = new ODataModel("/V2/Northwind/Northwind.svc/");
            // this.getView().setModel(oModel);
        }
        
    });
});
